/**************************************************************************************************
*
* \file Prefix2.cpp
* \brief C++ Training - Function Programming Task
*
* Copyright (C) 2015-2021 Klaus Iglberger - All Rights Reserved
*
* This file is part of the C++ training by Klaus Iglberger. The file may only be used in the
* context of the C++ training or with explicit agreement by Klaus Iglberger.
*
* Task: Refactor the 'Prefix' exercise by means of a function that checks if one vector is
*       a prefix of another vector.
*
**************************************************************************************************/

#include <cstdlib>
#include <iostream>
#include <string>
#include <vector>


// TODO: Implement the 'isPrefix()' function


int main()
{
   /*
   std::vector<int> v1;
   std::vector<int> v2;

   // Reading in the first vector
   {
      int i = 0;
      while( std::cin.peek() != '\n' && std::cin >> i ) {
         v1.push_back( i );
      }

      std::cout << "v1 = (";
      for( int i : v1 ) {
         std::cout << " " << i;
      }
      std::cout << " )\n";
   }

   std::cin.get();

   // Reading in the second vector
   {
      int i = 0;
      while( std::cin.peek() != '\n' && std::cin >> i ) {
         v2.push_back( i );
      }

      std::cout << "v2 = (";
      for( int i : v2 ) {
         std::cout << " " << i;
      }
      std::cout << " )\n";
   }

   // Check if vector v1 is a prefix of vector v2
   if( isPrefix( v2, v1 ) ) {
      std::cout << "v1 is a prefix of v2!\n";
   }
   else {
      std::cout << "v1 is NOT a prefix of v2!\n";
   }

   // Check if vector v2 is a prefix of vector v1
   if( isPrefix( v1, v2 ) ) {
      std::cout << "v2 is a prefix of v1!\n";
   }
   else {
      std::cout << "v2 is NOT a prefix of v1!\n";
   }
   */

   return EXIT_SUCCESS;
}
